package io.pixelstudios.pixelstudioscore.api.block.entity;

import net.minecraft.block.entity.BlockEntityType;
import net.minecraft.registry.Registries;
import net.minecraft.registry.Registry;
import net.minecraft.util.Identifier;

import static io.pixelstudios.pixelstudioscore.PixelStudiosCore.MOD_ID;

public class ModBlockEntities {

    /*
    public static final BlockEntityType<CassetteBlockEntity> CASSETTE =
            Registry.register(
                    Registries.BLOCK_ENTITY_TYPE,
                    Identifier.of(MOD_ID, "cassette"),
                    BlockEntityType.Builder.create(CassetteBlockEntity::new, ModBlocks.CASSETE_BLOCK).build()
            );
    */

    public static void registerBlockEntities() {

    //    LOGGER.info("Registering Block Entities for {}", MOD_ID);

    }

}
